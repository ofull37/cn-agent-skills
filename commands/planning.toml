description = "将工作拆分为带有验收标准和依赖顺序的小型可验证任务"

prompt = """
调用 planning-and-task-breakdown 技能。

阅读现有的 spec（SPEC.md 或等价物）以及相关的代码库部分。然后：

1. 进入计划模式——只读，不做任何代码更改
2. 识别组件之间的依赖关系图
3. 垂直切分工作（每个任务一条完整路径，而不是水平分层）
4. 编写带有验收标准和验证步骤的任务
5. 在阶段之间添加检查点
6. 呈现计划以供人工评审

将计划保存到 tasks/plan.md，将任务列表保存到 tasks/todo.md。
"""
